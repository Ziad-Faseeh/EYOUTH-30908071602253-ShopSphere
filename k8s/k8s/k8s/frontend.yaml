apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopsphere-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shopsphere-frontend
  template:
    metadata:
      labels:
        app: shopsphere-frontend
    spec:
      containers:
      - name: frontend
        image: nginx:alpine
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: shopsphere-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP